# STEP.md

CNN Visualizer を小さく動く単位で段階的に実装していくための計画。各STEPは「完了したらブラウザで動作確認できる」粒度にしてある。上から順番に進める。詳細な背景・目的・制約は [Plan.md](Plan.md) と [CLAUDE.md](CLAUDE.md) を参照。

## 技術スタック

- UI: React + TypeScript + Vite
- 推論: TensorFlow.js（学習済みモデル / 中間層の活性化取得に使用）
- ホスティング: GitHub Pages（静的ビルドのみ、バックエンドなし）

## 進め方のルール

- 1STEPずつ実装し、完了したら動作確認してからチェックを付けて次に進む
- 各STEPはMVP範囲（[Plan.md](Plan.md) 参照）を超えて機能を先取りしない
- 手戻りが出たら、このファイルのSTEPを見直して更新する

---

## STEP 0: プロジェクト雛形

- [x] Vite + React + TypeScript でプロジェクトを作成
- [x] GitHub Pages向けのbase pathを設定できる状態にする（vite.config.ts、base: '/CNNVisualizer/'）
- [x] `npm run dev` でローカル起動し、空のページが表示されることを確認（`npm run build` も成功確認済み）

## STEP 1: 画像入力（アップロード）

- [x] 画像ファイルをアップロードするUIを実装（src/App.tsx）
- [x] アップロードした画像をプレビュー表示する

## STEP 2: 画像入力（カメラ撮影）

- [x] `<input type="file" accept="image/*" capture="environment">` でカメラ撮影に対応（iPhone/iOS Safari想定、src/App.tsx）
- [x] 撮影した画像もSTEP1と同じプレビューに表示できるようにする

## STEP 3: TensorFlow.jsの導入と1モデルでの推論

- [x] TensorFlow.jsをセットアップ（@tensorflow/tfjs）
- [x] 学習済みモデル（MobileNet v1 alpha 0.25、Keras/LayersModel形式）を1つ読み込む
- [x] アップロード/撮影した画像を推論し、分類結果（ラベルと確率）をリスト表示する
- 備考: 当初`@tensorflow-models/mobilenet`（GraphModel）を使っていたが、STEP5で中間層の活性化を取得する必要が生じたため`tf.loadLayersModel()`に切り替えた（GraphModelは`classify()`/`infer()`しか公開しておらず、任意の層の出力を取り出せなかったため）。分類ロジック・前処理・ImageNetラベル一覧（`src/data/imagenetClasses.ts`、元パッケージから抽出、Apache License 2.0）は自前実装に置き換え済み（`src/mobilenet.ts`）

## STEP 4: モデル切り替え機能

- [ ] 切り替え可能な学習済みモデルを2〜3種類用意する（LayersModel形式で中間層アクセスできるものを選定）
- [ ] モデル選択UI（ドロップダウン等）を実装
- [ ] 選択中のモデルで推論結果が切り替わることを確認する

## STEP 5: 中間層の活性化を取得する

※STEP4より先に実施（ユーザー判断、2026-08-05）

- [x] 選択中モデルの各層（中間層）の出力を取得する仕組みを実装（`buildActivationModel`/`getActivationShapes`、`src/mobilenet.ts`）
- [x] まずはコンソール等で、層ごとの出力テンソルの形状を確認できる状態にする（56層、空間サイズ112→7・チャンネル数8→256と進む様子をブラウザ実行で確認済み）

## STEP 6: 活性化を画像として可視化する

- [x] 1つの層の活性化マップをcanvasに描画する（`drawActivationOverlay`で青→緑→赤のヒートマップ色に変換、src/heatmap.ts）
- [x] 元画像の上にオーバーレイ表示する（透過重ね合わせ。活性化が弱い部分ほどalphaを下げて元画像を透かす。現在は固定で`conv_pw_5_relu`層を表示、層切り替えはSTEP7で対応）
- 備考: 当初はチャンネル方向の単純平均（`getLayerHeatmap`）だったが、STEP7.5で「予測結果への寄与度を反映したい」というユーザー要望を受けGrad-CAMに置き換えた。詳細はSTEP7.5参照

## STEP 7: 層の切り替えUI（矢印ボタン）

- [x] 複数層の活性化オーバーレイを切り替えられるようにする（`OVERLAY_LAYERS`＝MobileNetV1の各ブロック出力14層に絞って対象に、src/App.tsx, src/mobilenet.ts）
- [x] 現在表示している層が何層目かをUIで分かるようにする（「層 X / 14」インジケーター表示）
- 備考: 当初は左右スワイプ＋矢印ボタンの両方に対応していたが、スワイプ操作が直感的でなく誤操作を招くとのユーザー指摘（2026-08-05）を受けスワイプは無効化し、矢印ボタンのみに一本化した

## STEP 7.5: 個別ニューロンの活性化グリッド表示（MVP範囲外の追加機能、ユーザー要望により2026-08-05実施）

チャンネル平均のオーバーレイだけでなく、選択中の層の各チャンネル（ニューロン）の活性化を個別のタイルとして並べて見られるようにした。

- [x] 「個別ニューロンを見る」ボタンでオーバーレイ表示とグリッド表示を切り替え（src/App.tsx）
- [x] 各チャンネルをチャンネルごとに独立して0〜1正規化し、タイル状に描画（`getChannelHeatmaps`, src/mobilenet.ts / `drawChannelTile`, src/heatmap.ts）
- [x] タイルは`minmax(64px, 1fr)`のCSS Gridで視認性を確保しつつ、チャンネル数が多い層（最大256）も縦スクロールで全件表示
- [x] ブラウザ実行（iPhone 13ビューポート、Playwright）で8チャンネル/256チャンネルどちらも件数・タイルサイズ（約67px四方）を確認済み
- [x] 層が深くなるほど活性化マップの解像度が下がり何に反応しているか分かりづらくなるため、各タイルの背景にモノクロ化した元画像を敷いてヒートマップを半透明合成（`drawChannelTile`, src/heatmap.ts）。グレースケール変換はタイルの数（最大256）だけやり直すと重いため、画像ごとに1回だけ生成してcanvasにキャッシュし、各タイルへはその縮小コピー（drawImageのスケーリング）のみで済ませるよう最適化（ユーザー指摘により2026-08-05対応）
- [x] 1枚に合成するオーバーレイをGrad-CAMに変更（ユーザー質問「一枚の活性化画像は判断結果の重みを乗算して合成したものか」への回答を経て2026-08-05対応）。予測クラスのスコアを対象層の活性化で微分し、チャンネルごとの重要度（寄与度）で重み付き合成することで「予測にどれだけ効いたか」を可視化。単純平均より分類根拠に忠実
  - `buildGradCamModels`：MobileNetV1は分岐のない一本道構造なので、対象層より後ろの層だけを繋ぎ直した「後半モデル」を層ごとに事前構築（src/mobilenet.ts）
  - `analyzeAllLayers`（後にチャンネル別ヒートマップ計算と統合、詳細は後述）：`tf.grad`で予測スコアを活性化で微分し、チャンネル重み`channelWeights`を算出。重み付き合成→ReLUで正の寄与のみ残し0〜1正規化（src/mobilenet.ts）
  - 個別ニューロングリッドの各タイルは、同じ`channelWeights`を0〜1に正規化した「寄与度」を枠線の太さ・色の濃さに連続的に反映（上位n%のような閾値カットではなく、CSS `color-mix()` とCSSカスタムプロパティ`--prominence`でグラデーション表現。ユーザー指摘により2026-08-05対応、src/App.tsx, src/App.css）
  - ブラウザ実行（iPhone 13ビューポート、Playwright）で確認：浅い層は薄く広いオーバーレイ、深い層（conv_pw_13_relu）では対象物の形状がはっきり赤く強調される、教科書的なGrad-CAMの挙動を確認
- [x] バグ修正：タイルの背景画像がヒートマップと同じ低解像度（深い層では7x7等）で描画されており、拡大表示時にぼやけて「どこに反応したか分かる」という本来の役割を果たしていなかった（ユーザー指摘により2026-08-05対応）。canvasの描画解像度を128x128に固定し、背景は元画像の解像度からそのまま縮小、ヒートマップだけを128x128へ拡大描画するよう修正（`drawChannelTile`, src/heatmap.ts）。ブラウザ実行で背景の細かい模様までくっきり見えることを確認
- [x] パフォーマンス改善①：層切り替えのたびにGrad-CAMを再計算しており重かった（ユーザー指摘により2026-08-05対応）。原因は(1)活性化モデルが毎回ほぼ全層分を計算、(2)予測クラスの特定に後半モデルの推論を毎回2回走らせていたこと。「画像は同じなら結果も同じ」というユーザーの指摘を踏まえ、画像読み込み時に14層すべてのGrad-CAMを一括計算（活性化の計算と予測クラスの特定は1回だけ、各層の勾配計算のみ繰り返す）し、層切り替えは計算済み結果を描画するだけに変更（src/mobilenet.ts, src/App.tsx）。ブラウザ実行で層切り替えが約290ms/回→約19ms/回に高速化したことを確認
- [x] パフォーマンス改善②：個別ニューロングリッドの層切り替えも同じ原因（`activationModel.predict`を毎回re-run）で重かった（ユーザー指摘により2026-08-05対応）。Grad-CAM計算と`activationModel.predict`の結果を共有できるため、`getGradCamForAllLayers`と`getChannelHeatmaps`を`analyzeAllLayers`に統合し、画像読み込み時にGrad-CAMとチャンネル別ヒートマップを1回の解析でまとめて計算・キャッシュする方式に変更（`LayerAnalysis`型、src/mobilenet.ts）。ブラウザ実行でグリッド表示中の層切り替えが約36ms/回（256タイルの再描画込み）まで高速化したことを確認
- [x] UI改善：解析中であることを画像の上に重ねて表示するように変更（従来は下部に別テキストで表示。ユーザー指摘により2026-08-05対応）。文言は「推論中」ではなく「解析中…」を採用（Grad-CAMは推論＝順伝播だけでなく勾配計算＝逆伝播も伴うため、教育アプリとして分かりやすい`解析中`とした）。`.processing-overlay`として画像に半透明の黒背景＋白文字で表示（src/App.tsx, src/App.css）
- [x] パフォーマンス改善③：計算はキャッシュ済みなのに、iPhone実機では個別ニューロングリッドの層14（256チャンネル）あたりが依然重いとのユーザー指摘（2026-08-05）。原因は`getGrayscaleSource`（src/heatmap.ts）が元画像を**そのままの解像度**（iPhone写真だと3000〜4000px級）でキャッシュしており、タイル1枚描画するたびにその巨大な画像から128x128へ縮小描画する処理を最大256回繰り返していたこと。グレースケール化と同時にタイル解像度(128x128)まで縮小した状態でキャッシュするよう変更し、各タイルは等倍コピーだけで済むようにした。検証用に3024x4032の大きな画像で計測したところ、グリッド初回表示が169ms→78ms、再表示が約90ms→約58msに改善（Playwright/Chromium計測。実機のモバイルSafariは画像縮小処理の最適化度合いがChromiumと異なるため、体感差はより大きくなる見込み）
- [x] バグ修正（第1弾）：個別ニューロン表示で強い紫枠（重要度が高い）のニューロンが、合成画像（Grad-CAM）では強調されていないことがあるとのユーザー指摘（2026-08-05）。原因は、枠線の強調度に`channelWeights`（勾配ベースの「重要度」＝重み単体）を使っていたため。重みは「そのチャンネルが発火していれば予測にどれだけ効くか」という感度でしかなく、実際にそのチャンネルが今回の画像でどれだけ発火したかは反映していなかった。個別タイルの活性化はチャンネルごとに独立して0〜1正規化されるため、実際はほとんど発火していないチャンネルも見た目は「真っ赤」になり得て、これが枠線とのズレの原因だった。合成画像自体は`Σ(重み×活性化)`で計算されているため、枠線の指標も「重み×活性化の空間最大値」＝実際の寄与度（`channelContributions`）に変更し、両者を整合させた（`GradCamResult`, src/mobilenet.ts / src/App.tsx）
- [x] バグ修正（第2弾・本質的な原因）：第1弾の修正後もユーザーの実機（iPhone）検証で「強い紫枠のタイルが赤く示す場所が、合成画像で強調されていない」ズレが再現した（2026-08-08）。実例画像で確認したところ、より根本的な問題は2つ：
  1. **枠線の指標自体の弱点**：`channelContributions`（そのチャンネル単体のピーク寄与度）は、そのピーク地点が合成ヒートマップの強調領域と別の場所にあっても高い値になり得ていた。合成ヒートマップは`cam = Σ_c weighted_c`（全チャンネルの重み付き活性化の合計）で決まるため、「単体で強い」ことと「合成画像のその場所が実際に赤くなる」ことは別物だった。そこで寄与度の定義を「合成ヒートマップが実際に強調している領域に、このチャンネルがどれだけ乗っているか」（`Σ_{x,y} weighted_c(x,y) × normalizedCam(x,y)`）に変更。これはcamの定義そのものを分解した量なので、「寄与度が高いチャンネルの合計は合成画像の赤い領域を説明する」という整合性が数式的に保証される
  2. **個別タイルの色そのものの誤解を招く表現**（実例画像で見つかった、より影響の大きい原因）：個別ニューロンのヒートマップはチャンネルごとに独立して0〜1正規化していたため、実際の活性化量がどれだけ小さいチャンネルでも「そのチャンネルの中で一番強い場所」が必ず真っ赤に表示されていた。その結果、ほぼ無反応で予測に関係ないチャンネルでも画面全体が真っ赤に見えることがあり、「赤い＝重要」という直感的な見方と枠線（重要度）が食い違って見える一番の原因になっていた。正規化の基準を「チャンネルごと」から「その層の全チャンネル・全ピクセルに共通の基準」に変更し、赤さの強さがチャンネル間で比較可能な「実際の反応の強さ」を表すようにした（`analyzeAllLayers`, src/mobilenet.ts）
  - ブラウザ実行（iPhone 13ビューポート、Playwright）で確認：深い層（256チャンネル）でほとんどのタイルが薄暗く表示され、実際に強く反応しているチャンネルだけが赤く目立つようになった。寄与度上位のタイルは合成画像の強調領域と同じような形状のパターンを示すことを確認
  - **その後、実写真では成り立たないことが判明**（2026-08-08）：ユーザーが用意した実写真（博物館の展示物、鳥、イグアナ）で試すと、層14のグリッドは信頼度の高低に関わらずほとんどのタイルが大きく赤くなった。数値で確認したところ、これは正規化のバグではなく「情報量の多い実写真だと、256チャンネル中の多く（7〜8割程度）が7x7の特徴マップのどこか1点では強く反応する」という実際の性質。一方、赤さ（反応の強さ）と寄与度の相関は弱い（相関係数0.2〜0.35）ことも確認でき、「強く反応すること」と「今回の分類に効くこと」は別概念であるとあらためて分かった。合成画像（Grad-CAM）自体は3枚とも意味のある部位（イグアナの背中の棘、鳥の胸、展示物の一部）にきれいに集中しており、原理的には問題なし
- [x] 個別ニューロン表示の再設計＋WebGL化：上記の通り「枠線＝寄与度、色＝反応の強さ」という2つの別々の指標を並べて見せる今の設計では、実写真だとほぼ全タイルが赤くなり「赤い＝重要」という誤解を招きやすいとユーザーと合意（2026-08-08）。**寄与度を枠線ではなく色の鮮やかさそのものに反映**させる方式に変更：寄与度が低いニューロンは、どれだけ強く反応していても色が沈んでグレー（背景のモノクロ画像）に近づき、寄与度が高いものだけが鮮やかな色で浮き上がる（`alpha = 活性化の強さ × 寄与度`でグレー画像と合成）。合わせて、枠線による強調は色に統合されたため廃止しシンプル化
  - 描画方式もCanvas2Dでのタイル1枚ずつの`drawImage`（最大256回）から、**WebGL2による一括描画**に変更（ユーザー指示）。全チャンネル分の頂点（位置・texcoord・チャンネル番号・寄与度）を1本の頂点バッファにまとめ、各チャンネルの活性化マップは`TEXTURE_2D_ARRAY`（チャンネルごとに1レイヤー）に格納し、背景のモノクロ画像テクスチャと合わせて**1回のバインド・1回のdraw callでグリッド全体を描画**する（`src/channelGridRenderer.ts`、色の配色・合成ロジックはフラグメントシェーダーに移植）。グリッドは1枚の`<canvas>`になり、コンテナ幅から列数・タイルサイズを算出してレスポンシブに対応（`ResizeObserver`で画面回転にも追随）。モバイルGPUのレンダーバッファ上限を考慮し、canvasの実解像度に安全マージンを設けた
  - バグ修正：実装直後、寄与度1.0のチャンネルも含めグリッド全体が無彩色にしかならない不具合が発生。原因は`UNPACK_ALIGNMENT`のデフォルト値(4)。活性化マップの幅（深い層では7pxなど）が4の倍数でないことが多く、テクスチャ配列へのアップロード時に行の境界がずれてデータが化けていた。`gl.pixelStorei(gl.UNPACK_ALIGNMENT, 1)`を追加して解決
  - ブラウザ実行（iPhone 13ビューポート、Playwright）で確認：実写真（イグアナ、寄与度1.0のチャンネルあり）でグリッドを開くと、ほとんどのタイルはグレーに沈み、寄与度が高い一握りのチャンネルだけが鮮やかに色付くことを確認（グリッド内の彩度ピクセルを数値検査）。8チャンネルの浅い層・256チャンネルの深い層のどちらでも正常に描画され、エラーなし。層切り替え（グリッド表示中）は概ね60〜110ms程度
  - パフォーマンス検証（ユーザー指摘「描画枚数によらず速いはず」を検証、2026-08-08）：`gl.finish()`でGPU完了を待った上で実測したところ、8ch/64ch/128ch/256chいずれも描画コストは約4〜8msでほぼ一定だった。1回のdraw callにまとめているため、チャンネル数が32倍になっても支配的なのはフレームごとの固定コストであり、タイル枚数にはほとんど左右されないことを確認。旧Canvas2D版（256タイルで約36ms/回）と比べ、チャンネル数が多い層ほど新方式の優位性が効く
  - バグ修正：ユーザーの実機検証で「背景がモノクロになっていない（カラーのまま）」との指摘（2026-08-08）。原因はグレースケール化に使っていたCanvas2Dの`ctx.filter = 'grayscale(1)'`がSafariでサポートが不安定なこと（Chromiumでは問題なく動くため、こちらの検証では気づけなかった）。ブラウザ互換性に依存しないよう、背景は色つきのまま縮小してキャッシュし（`getScaledSource`、旧`getGrayscaleSource`から改名）、グレースケール化はWebGLのフラグメントシェーダー側で輝度変換（`dot(color, vec3(0.299, 0.587, 0.114))`）して行うように変更（src/heatmap.ts, src/channelGridRenderer.ts）
- [x] 個別ニューロン表示の設計を一部差し戻し：「色の鮮やかさ＝寄与度」方式にしたところ、寄与度が低いニューロンがほぼグレーになり「どのパターンにどれだけ反応する回路が存在するか」という情報量が大きく失われたとユーザーからフィードバック（2026-08-08）。**色の濃さは寄与度と切り離し、そのチャンネル自身がどれだけ反応したかだけを表す**（`alpha = 活性化の強さ`、寄与度を掛けない）方式に戻した上で、**寄与度は改めて枠線の太さ・濃さで表現**するよう変更。ただしグリッドはCanvas2Dの個別`<canvas>`要素には戻さず、WebGL化した1枚のcanvas内で完結させるため、枠線もフラグメントシェーダーで描画：タイルのtexcoordが端からのしきい値（`(1px + 寄与度×3px) / タイル実ピクセルサイズ`）以内なら枠の色（`mix(--border, --accent, 寄与度)`）を出す方式にした。枠の色はCSSカスタムプロパティ（ライト/ダークモードで異なる`--accent`・`--border`）を`getComputedStyle`で読み取り、シェーダーのuniformとして渡している（src/channelGridRenderer.ts）
  - ブラウザ実行で確認：色の濃淡で全チャンネルの反応パターンが見えるようになり、かつ寄与度が高いタイルには紫の枠線が表示されることを確認

## STEP 8: UI/UXの磨き込み

- [ ] シンプルさを保ったまま、全体のレイアウト・配色・操作性を整える
- [ ] モデル選択〜画像入力〜結果確認〜層プレビューの一連の流れがスムーズか確認する

## STEP 9: GitHub Pagesへのデプロイ

- [x] GitHub Actionsで自動デプロイするworkflowを作成（.github/workflows/deploy.yml）※iPhone実機テストのためSTEP2完了時点で前倒しで実施
- [x] リポジトリ設定でPagesのSourceを「GitHub Actions」にする
- [x] 本番ビルドを作成し、GitHub Pages上で正しく動作することを確認する（iPhone実機でカメラ撮影〜プレビューまで確認済み）
- [ ] READMEに使い方・デプロイURLを記載する（MVP完成時にまとめて記載）

---

## MVP完了後のバックログ（優先度未定、着手はMVP完成後）

- [x] 分類結果ラベルの日本語化（2026-08-09対応）：ImageNet1000クラスの日本語訳を[gist](https://gist.github.com/PonDad/4dcb4b242b9358e524b4ddecbee385e9)から取得し、`src/data/imagenetClassesJa.ts`として追加（`imagenetClasses.ts`と同じクラスindex順）。ただし元データに同綴異義語の取り違えによる明らかな誤訳が多数見つかったため（例: `cock`（雄鶏）→「コック」、`kite`（トビ）→「凧」、`organ`（オルガン）→「器官」、`safe`（金庫）→「安全」、`vacuum`（掃除機）→「真空」、`badger`（アナグマ）→「狸」、`iPod`→「アイフォーン」、`pick`/`pickup`/`upright`等の名詞を動詞・形容詞の意味に誤訳、等）、目視確認できた約40件を手作業で修正してから組み込んだ。`Prediction`型に`classNameJa`を追加し（`src/mobilenet.ts`）、分類結果は日本語ラベルを主表示、英語ラベルを小さく併記する形にした（`src/App.tsx`, `src/App.css`）。1000件すべてを検証しきれてはいないため、実機確認で気になる訳があれば都度修正する

---

MVP完了後の追加機能はここには含めない。必要になった時点で別途検討する。
